.content-header_right {
    img {
        width: 48px;
        height: 48px;
        margin-right: 15px;
        border-radius: 50%;
    }

    .title {
        font-size: 20px;
        font-weight: bold;
    }
}

.breadcrumbs_dot {
    display: inline-block;
    width: 3px;
    height: 3px;
    border-radius: 50%;
    background: #000;
    margin: 3px 10px;
    opacity: 0.5;
}